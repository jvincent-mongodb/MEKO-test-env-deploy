name: CI

on: [push]

jobs:
  copybara:
    runs-on: ubuntu-latest

    steps:
    
        - name: "build-copybara"
        - run: /bin/bash
          args:
            - -c
            - git
            - clone
            - --depth=1
            - https://github.com/google/copybara
